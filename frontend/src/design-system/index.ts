/* =============================================================================
   ServWave Design System — barrel re-export.
   Import design tokens from a single entry point: `@/design-system`.
   ============================================================================= */

export { token, chartPalette, severityRamp } from './tokens';
